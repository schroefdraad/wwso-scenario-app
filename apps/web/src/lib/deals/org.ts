/**
 * Vaste placeholder-org totdat taak 17 (Supabase Auth + Row Level Security op org_id) een
 * echte, per-gebruiker org_id levert. Harde regel 3 vraagt `org_id` vanaf regel één, ook met
 * één organisatie — vandaar dat dit al een kolom is en geen latere toevoeging.
 */
export const PLACEHOLDER_ORG_ID = '00000000-0000-0000-0000-000000000001';
